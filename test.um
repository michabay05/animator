fn sequence(): void {
    ax := axes();
    c := curve(ax);
    enable(ax);
    enable(c);
    /*
    color := Color{155, 10, 255, 255};
    r := rect(Vec2{0,0}, Vec2{1,1}, color);
    t := text("What is good bois?", Vec2{2,2}, 30, color);

    fade_in(r);
    fade_in(t);
    play();

    wait();
    play(3);

    move(r, Vec2{-4, -4});
    move(t, Vec2{4, 4});
    play();

    fade_out(r);
    fade_out(t);
    play();
    */
}
